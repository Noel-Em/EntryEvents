import { GEntry } from "./Entry/GlobalEntry"
import { Entry } from "./Entry/Entry"

export const EntryEvents = {
    GEntry: GEntry,           // Stands for Global Entry
    Entry: Entry              // Is simply Entry
}